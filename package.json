{
  "name": "caveat",
  "private": true,
  "version": "0.0.0",
  "description": "Long-term memory layer for Claude Code and Codex",
  "type": "module",
  "packageManager": "pnpm@10.0.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "corepack pnpm -r build",
    "test": "corepack pnpm -r test",
    "typecheck": "corepack pnpm -r typecheck",
    "check:npm-pack": "node scripts/check-npm-pack.mjs",
    "check:release-smoke": "node --check scripts/check-npm-pack.mjs && node --check scripts/codex-sidecar-advisory-smoke.mjs && corepack pnpm check:npm-pack && corepack pnpm smoke:codex-sidecar-advisory -- --help",
    "smoke:codex-sidecar-advisory": "node scripts/codex-sidecar-advisory-smoke.mjs",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.6",
    "typescript": "^6.0.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild"
    ]
  }
}
